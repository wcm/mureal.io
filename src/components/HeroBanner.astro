<section class="mx-auto max-w-4xl px-6 pt-5">
	<div class="hero-shader relative overflow-hidden rounded-xl px-6 py-28 text-center md:px-16">
		<div class="hero-shader__base"></div>
		<div class="hero-shader__aurora"></div>
		<div class="hero-shader__particles"></div>

		<div class="relative z-10">
			<h1 class="font-brand text-5xl leading-tight text-white">From idea to execution</h1>
			<p class="mx-auto mt-4 max-w-2xl text-base text-white/90 md:text-lg">We build tools to help you think, design, and build faster.</p>
		</div>
	</div>
</section>

<style>
	.hero-shader {
		background: #09090b;
		isolation: isolate;
	}

	.hero-shader__base,
	.hero-shader__aurora,
	.hero-shader__particles {
		position: absolute;
		inset: -20%;
		pointer-events: none;
	}

	.hero-shader__base {
		background:
			radial-gradient(circle at 15% 20%, rgba(56, 189, 248, 0.65), transparent 42%), radial-gradient(circle at 85% 80%, rgba(244, 114, 182, 0.6), transparent 44%),
			radial-gradient(circle at 52% 52%, rgba(139, 92, 246, 0.55), transparent 48%), linear-gradient(130deg, #0f172a 0%, #111827 45%, #1f1147 100%);
		filter: saturate(125%) contrast(112%);
		animation: shaderDrift 16s ease-in-out infinite alternate;
	}

	.hero-shader__aurora {
		background: conic-gradient(from 180deg at 35% 40%, rgba(45, 212, 191, 0.25), rgba(59, 130, 246, 0.2), rgba(244, 114, 182, 0.25), rgba(167, 139, 250, 0.25), rgba(45, 212, 191, 0.25));
		mix-blend-mode: screen;
		filter: blur(42px);
		animation: shaderSpin 22s linear infinite;
	}

	.hero-shader__particles {
		inset: 0;
		background-image: radial-gradient(circle, rgba(255, 255, 255, 0.75) 0 1.3px, transparent 1.5px), radial-gradient(circle, rgba(255, 255, 255, 0.55) 0 1.1px, transparent 1.4px);
		background-size:
			130px 130px,
			180px 180px;
		background-position:
			0 0,
			50px 70px;
		opacity: 0.22;
		animation: particlesFloat 14s linear infinite;
	}

	@keyframes shaderDrift {
		0% {
			transform: translate3d(-2%, -1%, 0) scale(1);
		}
		100% {
			transform: translate3d(2%, 1%, 0) scale(1.08);
		}
	}

	@keyframes shaderSpin {
		0% {
			transform: rotate(0deg) scale(1);
		}
		100% {
			transform: rotate(360deg) scale(1.06);
		}
	}

	@keyframes particlesFloat {
		0% {
			background-position:
				0 0,
				50px 70px;
		}
		100% {
			background-position:
				130px 130px,
				230px 250px;
		}
	}
</style>
